# Copy to .env for local dev, set as Railway environment variables for production

# Stripe keys (get from dashboard.stripe.com)
STRIPE_SECRET_KEY=sk_live_...
STRIPE_WEBHOOK_SECRET=whsec_...

# Admin dashboard credentials (change these!)
ADMIN_USER=admin
ADMIN_PASS=ChangeMe2025!

# Max devices per license (default)
MAX_DEVICES=10

# DB path
DB_PATH=licenses.db
